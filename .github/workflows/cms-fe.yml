name: swachhdnr_fe

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 */1 * * *"

jobs:
  pull-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source repository
        uses: actions/checkout@v2
        with:
          repository: s2pl/swachhdnr_fe
          token: ${{ secrets.PERSONAL_TOKEN  }}

      - name: Add source repository as upstream
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
        run: |
          git remote add upstream https://sarva-suvidhaen:$GITHUB_TOKEN@github.com/sarva-suvidhaen/cms-fe.git
          git remote add origin1 https://sarva-suvidhaen:$GITHUB_TOKEN@github.com/s2pl/swachhdnr_fe.git

      # - name: Pull from source repository
      #   run: |
      #     git pull upstream main

      - name: Push to destination repository
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Action"
          git config --unset-all http.https://github.com/.extraheader
          git remote -v
          git fetch origin
          git merge origin/main
          git push -f upstream main
